"""PrimiBlocks — a clonable scaffold for building domain-specific primitive kits.

Public surface is intentionally narrow: most users invoke the CLI
(`primiblocks`) or call `primiblocks.render.render()`. Internal modules
(`contract`, `templates`, `primitives`) are accessed transitively.
"""

__version__ = "0.1.0"
